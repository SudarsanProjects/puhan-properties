# render.yaml
services:
  - type: web
    name: puhan-properties
    env: node
    plan: free # You can change this to a paid plan for more resources
    buildCommand: "npm install"
    startCommand: "npm start"
    databases:
      - name: puhan-db
        plan: free
        region: oregon
    envVars:
      - key: NODE_VERSION
        value: "20" # Specify a recent, stable Node.js version
